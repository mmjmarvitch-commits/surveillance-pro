# ═══════════════════════════════════════════════════════════════════════════════
# SURVEILLANCE PRO - Configuration Production
# ═══════════════════════════════════════════════════════════════════════════════
# IMPORTANT: Copiez ce fichier vers .env et remplissez TOUTES les valeurs
# Ne commitez JAMAIS le fichier .env dans git !
# ═══════════════════════════════════════════════════════════════════════════════

# ─── Serveur ───
PORT=3000
NODE_ENV=production

# ─── Sécurité (OBLIGATOIRE en production) ───
# Générez avec: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGEZ_MOI_64_CARACTERES_MINIMUM_ALEATOIRES

# Clé de chiffrement des données sensibles (64 caractères hex)
# Générez avec: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
DEVICE_ENCRYPTION_KEY=CHANGEZ_MOI_64_CARACTERES_HEX

# Clé de chiffrement au repos (64 caractères hex)
DATA_ENCRYPTION_KEY=CHANGEZ_MOI_64_CARACTERES_HEX_DIFFERENT

# ─── Mot de passe admin initial (optionnel) ───
# Si non défini, un mot de passe aléatoire sera généré au premier démarrage
# ADMIN_PASSWORD=VotreMotDePasseSecurise123!

# ─── CORS - Origines autorisées (séparées par des virgules) ───
# Laissez vide pour autoriser toutes les origines (déconseillé en production)
# Exemple: https://dashboard.votredomaine.com,https://app.votredomaine.com
ALLOWED_ORIGINS=

# ─── Base de données Turso (optionnel, sinon SQLite local) ───
# TURSO_URL=libsql://votre-db.turso.io
# TURSO_TOKEN=votre-token-turso

# ─── Email (optionnel, pour les alertes) ───
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=votre@email.com
# SMTP_PASS=votre-mot-de-passe-app
# ALERT_EMAIL=admin@votreentreprise.com
