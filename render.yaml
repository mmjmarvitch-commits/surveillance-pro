services:
  - type: web
    name: surveillance-pro
    runtime: node
    region: frankfurt
    buildCommand: cd backend && npm ci
    startCommand: cd backend && node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        value: SupervisionPro2026SecretKey
